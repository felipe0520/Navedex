export interface NaverInterfaceUpdate {
  name: string;
  birthDate: string;
  jobRole: string;
  admissionDate: string;
  projects: string[];
}

export interface NaverAuthenticationData {
  id: string;
  token: string;
}
